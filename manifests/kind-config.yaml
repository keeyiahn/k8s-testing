kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # Flask 
      - containerPort: 32327
        hostPort: 32327
        protocol: TCP
      # External access to Kafka broker
      - containerPort: 30001
        hostPort: 30001
        protocol: TCP
  - role: worker

